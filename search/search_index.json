{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Documentation for pynxtools-ellips","text":"<p><code>pynxtools-ellips</code> is a free and open-source data software for harmonizing ellipsometry data and metadata for research data management using NeXus, implemented with the goal to make scientific research data FAIR (findable, accessible, interoperable, and reusable).</p> <p><code>pynxtools-ellips</code>, which is a plugin for pynxtools, provides a tool for reading data from (currently Woollam VASE) proprietary and open data formats from technology partners and the wider ellipsometry community. The tool standardizes these data such that it is compliant with the NeXus application definitions <code>NXoptical_spectroscopy</code> and its extension <code>NXellipsometry</code>. <code>pynxtools-ellips</code> is developed both as a standalone reader and as a tool within NOMAD, which is the open-source data management platform for materials science we are developing with FAIRmat.</p> <p><code>pynxtools-ellips</code> solves the challenge of using heterogeneous and unfindable data formats which is common in the field of ellipsometry.</p> <p><code>pynxtools-ellips</code> is useful for scientists from the ellipsometry community that deal with heterogeneous data, for technology partners and data providers looking for ways to make their data FAIRer, and for research groups that want to organize their data using NeXus and NOMAD.</p>  Contact  <p>For questions or suggestions:</p> <ul> <li>Open an issue on the <code>pynxtools-ellips</code> GitHub</li> <li>Join our Discord channel</li> <li>Get in contact with our lead developers.</li> </ul> Project and community <ul> <li>NOMAD code guidelines</li> </ul> <p>The work is funded by the Deutsche Forschungsgemeinschaft (DFG, German Research Foundation) - 460197019 (FAIRmat).</p>"},{"location":"index.html#tutorial","title":"Tutorial","text":"<ul> <li>Installation guide</li> <li>Standalone usage</li> <li>Development guide</li> </ul>"},{"location":"index.html#how-to-guides","title":"How-to guides","text":""},{"location":"index.html#learn","title":"Learn","text":"<ul> <li>Note on versioning</li> </ul>"},{"location":"index.html#reference","title":"Reference","text":"<p>Here you can learn which specific measurement setups and file formats from technology partners <code>pynxtools-ellips</code> currently supports.</p> <ul> <li>Woollam VASE</li> </ul>"},{"location":"contact.html","title":"Get in contact","text":"<p>NOMAD, <code>pynxtools</code>, and <code>pynxtools-ellips</code> are open source projects that warmly welcome community projects, contributions, suggestions, bug fixes, and constructive feedback. <code>pynxtools</code> is built mainly within FAIRmat Area B - Experiment.</p> <p>You can reach us through different channels. You can send an email directly to one of the main contributors:</p> Name E-mail Github profiles Dr. Lukas Pielsticker e-mail @lukaspie Dr. Markus K\u00fchbach e-mail @mkuehbach <p>Alternatively, you can also:</p> <ul> <li>Open an issue on the GitHub</li> <li>Join the NOMAD discord channel and ask us there directly.</li> </ul>"},{"location":"how-tos/convert_data.html","title":"Convert data from the command line","text":"<p>This is the terse reference version of Tutorial &gt; Convert your first ellipsometry dataset \u2014 copy-paste commands, no explanations.</p>"},{"location":"how-tos/convert_data.html#ja-woollam-vasecompleteease-dat-export-eln","title":"J.A. Woollam VASE/CompleteEASE <code>.dat</code> export + ELN","text":"<pre><code>pynx convert examples/eln_data.yaml examples/test-data.dat --reader ellips --nxdl NXellipsometry --output SiO2onSi_example.nxs\n</code></pre> <p>Both files are required: the ELN yaml declares the <code>.dat</code> file's column layout (<code>colnames</code>/<code>sep</code>/<code>skip</code>), so <code>pynx</code> can't parse the vendor file without it. Order doesn't matter \u2014 the reader always reads the ELN first.</p>"},{"location":"how-tos/convert_data.html#what-each-flag-does","title":"What each flag does","text":"<ul> <li><code>--reader ellips</code> \u2014 selects the <code>pynxtools-ellips</code> reader.</li> <li><code>--nxdl NXellipsometry</code> \u2014 the application definition the output should conform to.</li> <li><code>--output &lt;file&gt;.nxs</code> \u2014 where to write the result.</li> <li>the <code>.yaml</code> file \u2014 the ELN metadata (and <code>.dat</code> parser configuration), detected by its extension.</li> <li>the <code>.dat</code> file \u2014 the raw VASE/CompleteEASE export, detected by its extension.</li> </ul>"},{"location":"how-tos/convert_data.html#inspect-the-result","title":"Inspect the result","text":"<p>Open the generated <code>.nxs</code> file with H5Web, the VS Code H5Web extension, or any HDF5 viewer.</p>"},{"location":"learn/appdefs.html","title":"NXoptical_spectroscopy and NXellipsometry","text":"<p><code>NXellipsometry</code> is the NeXus application definition this reader converts into. It builds on the more generic <code>NXoptical_spectroscopy</code> base structure (entry, instrument, sample, user) and adds ellipsometry-specific concepts under <code>ENTRY/data_collection</code>, <code>ENTRY/instrument</code>, and <code>ENTRY/derived_parameters</code>.</p>"},{"location":"learn/appdefs.html#the-data_collection-group","title":"The <code>data_collection</code> group","text":"<p><code>ENTRY/data_collection</code> is an <code>NXdata</code> group holding both the raw measurement and its NeXus default-plot metadata:</p> <ul> <li><code>measured_data</code> \u2014 the full result set, shape <code>(N_measurements, N_observables, N_spectrum)</code>. For a VASE/CompleteEASE Psi/Delta scan, <code>N_measurements</code> is the number of angles of incidence, <code>N_observables</code> is 2 (Psi, Delta), and <code>N_spectrum</code> is the number of wavelength points.</li> <li><code>NAME_spectrum</code> (e.g. <code>wavelength_spectrum</code>) \u2014 the 1D spectral axis, length <code>N_spectrum</code>.</li> <li><code>DATA[*]</code> (e.g. <code>Psi_50deg</code>, <code>Delta_60deg</code>) \u2014 one field per (angle, observable) combination, each a 1D slice of <code>measured_data</code> along the spectrum dimension.</li> </ul>"},{"location":"learn/appdefs.html#why-the-default-plot-signal-must-be-1d-not-3d","title":"Why the default-plot signal must be 1D, not 3D","text":"<p>NeXus's <code>NXdata</code> convention requires the <code>@signal</code> field's shape to line up dimension-by-dimension with its <code>@axes</code> field(s) \u2014 a viewer like h5web reads <code>@signal</code>/<code>@axes</code> and expects <code>signal.shape[i] == len(axes[i])</code>. <code>measured_data</code> is intentionally 3D (the application definition says so explicitly), but <code>wavelength_spectrum</code> is 1D, so <code>measured_data</code> itself can never be the default-plot <code>@signal</code> \u2014 only a 1D slice of it can be.</p> <p>That's what the per-field <code>DATA[Psi_50deg]</code> etc. entries are for: each is written as an HDF5 virtual dataset (VDS) \u2014 a <code>{\"link\": \"/entry/data_collection/measured_data\", \"shape\": np.index_exp[angle_index, observable_index, :]}</code> template entry \u2014 a real, separate 1D dataset that shares storage with <code>measured_data</code> rather than copying it. <code>data_collection/@signal</code> points at the first of these (e.g. <code>Psi_50deg</code>), <code>@axes</code> points at <code>wavelength</code>, and <code>@auxiliary_signals</code> lists the rest, so h5web (or any NeXus-aware viewer) can render a proper multi-line default plot.</p>"},{"location":"learn/architecture.html","title":"Reader architecture","text":"<p><code>pynxtools-ellips</code> is a <code>pynxtools</code> reader plugin, built on <code>pynxtools</code>'s <code>MultiFormatReader</code>. This page explains how the pieces fit together and, more importantly, where a new vendor format would go.</p>"},{"location":"learn/architecture.html#one-reader-dispatched-by-file-extension","title":"One reader, dispatched by file extension","text":"<p><code>EllipsometryReader</code> (<code>src/pynxtools_ellips/reader.py</code>) registers as the <code>ellips</code> entry point. It routes input files by extension:</p> Extension Handler What it does <code>.yaml</code> / <code>.yml</code> <code>handle_eln_file</code> Reads the ELN metadata file \u2014 both for NeXus template content (flattened via <code>CONVERT_DICT</code>/<code>REPLACE_NESTED</code>, kept on the reader unchanged from earlier versions of this plugin) and for the parser configuration (<code>colnames</code>/<code>sep</code>/<code>skip</code>/<code>filename</code>) the <code>.dat</code> file needs to be read at all. <code>.dat</code> <code>handle_dat_file</code> Parses a J.A. Woollam VASE/CompleteEASE export via <code>WoollamParser</code>. <p>The ELN must be processed first \u2014 <code>self.processing_order = [\".yaml\", \".yml\", \".dat\"]</code> guarantees that regardless of the order files are passed on the command line, since the <code>.dat</code> file's column layout isn't self-describing and has to come from the ELN.</p>"},{"location":"learn/architecture.html#how-the-eln-is-handled","title":"How the ELN is handled","text":"<p>Most <code>pynxtools</code> plugins route all NeXus content \u2014 ELN-sourced and instrument-file-sourced alike \u2014 through the config file's <code>@eln</code>/<code>@attrs</code>/<code>@data</code> tokens, with every path spelled out explicitly. <code>pynxtools-ellips</code> only does that for the handful of paths that come from the parsed <code>.dat</code> file (see below); the ELN's own content is written directly by <code>handle_eln_file</code> via the existing <code>flatten_and_replace</code>/<code>CONVERT_DICT</code> mechanism.</p>"},{"location":"learn/architecture.html#the-config-file-and-wildcards","title":"The config file and wildcards","text":"<p><code>config/config_woollam.json</code> only covers what the ELN's own structure can't: the parsed measurement data, and the repeated per-angle/per-observable fields (<code>Psi_50deg</code>, <code>Delta_60deg</code>, ...). The repeated fields are added using <code>MultiFormatReader</code>'s wildcard mechanism:</p> <pre><code>\"/ENTRY[entry]/data_collection/DATA[*]\": \"@data:*\",\n\"/ENTRY[entry]/data_collection/DATA[*]/@units\": \"degree\",\n\"/ENTRY[entry]/data_collection/FIELDNAME_errors[*_errors]\": \"@data:*_errors\"\n</code></pre> <p><code>EllipsometryReader.get_data_dims()</code> returns the parser's list of field labels (<code>[\"Psi_50deg\", \"Psi_60deg\", ..., \"Delta_70deg\"]</code>); for each label, <code>pynxtools</code> expands <code>*</code> in both the key and the value, so <code>DATA[*]</code> becomes one concrete template entry per label.</p>"},{"location":"learn/architecture.html#the-parser","title":"The parser","text":"<p><code>WoollamParser</code> (<code>src/pynxtools_ellips/parsers/woollam.py</code>) subclasses a small shared base, <code>_EllipsParser</code> (<code>parsers/base.py</code>):</p> <ul> <li><code>matches_file(file)</code> \u2014 a cheap structural check (the acquisition-method marker <code>VASEmethod[</code> near the top of the file), not just an extension check.</li> <li><code>_parse(file, header_config=...)</code> \u2014 populates <code>self.data</code> with the measurement arrays and, for each per-angle/per-observable field, a <code>{\"link\": ..., \"shape\": np.index_exp[...]}</code> entry requesting a sliced HDF5 virtual dataset \u2014 see Learn &gt; Application definitions for why this has to be a slice rather than the full array.</li> <li><code>post_process(eln_data)</code> \u2014 derives <code>atom_types</code> from the sample's chemical formula if the ELN didn't already provide one.</li> </ul> <p>If a second vendor parser is added, <code>parsers/base.py</code> is the contract to implement against </p>"},{"location":"learn/architecture.html#adding-a-second-vendor","title":"Adding a second vendor","text":"<ol> <li>Add a new parser module under <code>parsers/</code>, subclassing <code>_EllipsParser</code>.</li> <li>Register its file extension in <code>EllipsometryReader.__init__</code>'s <code>self.extensions</code>.</li> <li>If its data doesn't fit the existing <code>config_woollam.json</code> mapping, add a new config file and a way to select it in the reader.</li> <li>Add real vendor example data under <code>examples/&lt;vendor&gt;/</code>, following the pattern in Tutorial &gt; Convert your first ellipsometry dataset.</li> </ol>"},{"location":"learn/versioning.html","title":"Note on versioning","text":"<p>Keeping track of versions is essential for every software and semantic data modeling activity. We follow PEP440 when reporting versions, fetching metadata at run time using <code>importlib.metadata</code>. Several versions need to be distinguished. The version of <code>pynxtools</code>, the one of the <code>NeXus definitions</code>, and those of <code>h5py</code> and the <code>HDF5</code> library. See the respective section of the <code>pynxtools</code> documentation that explains where these versions can be found.</p>"},{"location":"learn/versioning.html#pynxtools-plugin-version","title":"<code>pynxtools-plugin</code> version","text":"<p>We consider it best practice that also each plugin of <code>pynxtools</code> writes its version as a <code>programID</code> instance of <code>NXprogram</code>. <code>ID</code> serves as a placeholder that can be either omitted or replaced by an integer if multiple programs need to be distinguished. Counting should start at one, i.e., <code>program</code> or <code>program1</code> are preferred options. Specifically, the field <code>program</code> inside the <code>programID</code> instance should store the distribution (package) name. The attribute <code>program/@version</code> should store a version string following PEP440.</p>"},{"location":"reference/vase.html","title":"Data from Woollam instruments","text":"<p>The reader supports VASE/CompleteEASE ellipsometry files exported from J.A. Woollam instruments in the <code>.dat</code> format, via <code>WoollamParser</code>.</p> <ul> <li>Parser: <code>src/pynxtools_ellips/parsers/woollam.py</code></li> <li>Config file: <code>src/pynxtools_ellips/config/config_woollam.json</code></li> </ul> <p>See Learn &gt; Reader architecture for how the pieces fit together.</p>"},{"location":"reference/vase.html#example","title":"Example","text":"<p>An example dataset can be found here. Both the ELN file and the <code>.dat</code> data file need to be passed:</p> <pre><code>pynx convert examples/eln_data.yaml examples/test-data.dat --reader ellips --nxdl NXellipsometry --output SiO2onSi.nxs\n</code></pre> <p>See Tutorial &gt; Convert your first ellipsometry dataset for a walkthrough.</p>"},{"location":"reference/vase.html#acknowledgments","title":"Acknowledgments","text":"<p>We thank Carola Emminger and Chris Sturm for providing the implementation and respective example data for this reader.</p>"},{"location":"tutorial/contributing.html","title":"Development guide","text":"<p>This tutorial will guide you through on how to set up a working environment for developing <code>pynxtools-ellips</code>.</p>"},{"location":"tutorial/contributing.html#what-should-you-know-before-this-tutorial","title":"What should you know before this tutorial?","text":"<ul> <li>You should read the guide on getting started with <code>pynxtools</code>.</li> <li>You should read the installation tutorial.</li> </ul>"},{"location":"tutorial/contributing.html#what-you-will-know-at-the-end-of-this-tutorial","title":"What you will know at the end of this tutorial?","text":"<p>You will know</p> <ul> <li>how to setup your environment for developing <code>pynxtools-ellips</code></li> <li>how to make changes to the software</li> <li>how to test the software</li> <li>how to contribute on GitHub</li> <li>how to use <code>pynxtools-ellips</code> as a NOMAD plugin</li> </ul>"},{"location":"tutorial/contributing.html#contributing","title":"Contributing","text":"Structure of the <code>pynxtools-ellips</code> repository <p>The software tools are located inside <code>src/pynxtools_ellips</code>. They are shipped with unit tests located in <code>tests</code>.</p>"},{"location":"tutorial/contributing.html#setup","title":"Setup","text":"<p>It is recommended to use python 3.12 with a dedicated virtual environment for this package. Learn how to manage python versions and virtual environments. We recommend using <code>uv</code>, an extremely fast Python package and project manager. In this tutorial, you will find parallel descriptions, using either <code>uv</code> or a more classical approach using <code>venv</code> and <code>pip</code>.</p> <p>Start by creating a virtual environment:</p> uvvenv <p><code>uv</code> is capable of creating a virtual environment and install the required Python version at the same time.</p> <pre><code>uv venv --python 3.12\n</code></pre> <p>Note that you will need to install the Python version manually beforehand.</p> <pre><code>python -m venv .venv\n</code></pre> <p>That command creates a new virtual environment in a directory called .venv.</p>"},{"location":"tutorial/contributing.html#development-installation","title":"Development installation","text":"<p>We start by cloning the repository:</p> <pre><code>git clone https://github.com/FAIRmat-NFDI/pynxtools-ellips.git \\\\\n    --branch main \\\\\n    --recursive pynxtools-ellips\ncd pynxtools-ellips\n</code></pre> <p>Next, we install the package in editable mode (together with its dependencies):</p> uvpip <pre><code>uv pip install -e \".[dev]\"\n</code></pre> <p>Note that you will need to install the Python version manually beforehand.</p> <pre><code>pip install --upgrade pip\npip install -e \".[dev]\"\n</code></pre>"},{"location":"tutorial/contributing.html#linting-and-formatting","title":"Linting and formatting","text":"<p>We are using ruff and mypy for linting, formatting, and type checking. It is recommended to use the pre-commit hook available for ruff which formats the code and checks the linting before making a Git commit.</p> <p>Install the precommit by running</p> <pre><code>pre-commit install\n</code></pre> <p>from the root of this repository.</p>"},{"location":"tutorial/contributing.html#testing","title":"Testing","text":"<p>There exist unit tests for the software written in pytest which can be used as follows:</p> <pre><code>pytest -sv tests\n</code></pre>"},{"location":"tutorial/contributing.html#editing-the-documentation","title":"Editing the documentation","text":"<p>We are using mkdocs for the documentation. If you edit the documentation, you can build it locally. For this, you need to install an additional set of dependencies:</p> uvpip <pre><code>uv pip install -e \".[docs]\"\n</code></pre> <pre><code>pip install -e \".[docs]\"\n</code></pre> <p>You can then serve the documentation locally by running</p> <pre><code>mkdocs serve\n</code></pre>"},{"location":"tutorial/contributing.html#contributing-to-the-package-on-github","title":"Contributing to the package on GitHub","text":"<p>Once you are happy with the changes, please commit them on a separate branch and create a pull request on GitHub. We run a number of GitHub actions that check the correct linting, run the tests in an isolated environment, and build the documentation. Once these pass and a peer review of the code has occurred, your code will be accepted.</p>"},{"location":"tutorial/contributing.html#developing-pynxtools-ellips-as-a-nomad-plugin","title":"Developing <code>pynxtools-ellips</code> as a NOMAD plugin","text":"<p>If you plan to contribute to the NOMAD plugin functionality of pynxtools-ellips, it often makes sense to use the NOMAD development environment called <code>nomad-distro-dev</code>. You can learn more in the NOMAD documentation.</p>"},{"location":"tutorial/contributing.html#troubleshooting","title":"Troubleshooting","text":"<p>If you face any issues with the tool or when setting up the development environment, please create a new Github Issue.</p>"},{"location":"tutorial/convert_your_first_dataset.html","title":"Convert your first ellipsometry dataset","text":""},{"location":"tutorial/convert_your_first_dataset.html#who-is-this-tutorial-for","title":"Who is this tutorial for?","text":"<p>Anyone who has <code>pynxtools-ellips</code> installed and wants to see, hands-on, how an ellipsometry measurement turns into a standardized <code>NXellipsometry</code> file.</p>"},{"location":"tutorial/convert_your_first_dataset.html#what-should-you-know-before-this-tutorial","title":"What should you know before this tutorial?","text":"<ul> <li>You should have <code>pynxtools-ellips</code> installed \u2014 see the installation guide.</li> <li>You should have a copy of the <code>pynxtools-ellips</code> repository, since this tutorial uses the example files that ship with it.</li> </ul>"},{"location":"tutorial/convert_your_first_dataset.html#what-will-you-know-at-the-end-of-this-tutorial","title":"What will you know at the end of this tutorial?","text":"<ul> <li>How the pieces of a conversion \u2014 raw data, an ELN file, and the <code>pynx convert</code> command \u2014 fit together.</li> <li>What a converted <code>NXellipsometry</code> file looks like.</li> <li>Where to go if you want to add a second vendor format.</li> </ul>"},{"location":"tutorial/convert_your_first_dataset.html#the-example-dataset","title":"The example dataset","text":"<p>The repository ships a small, self-contained example under <code>examples/</code>: a J.A. Woollam VASE/CompleteEASE Psi/Delta scan of a 2nm SiO2 layer on Si, measured on an RC2 ellipsometer.</p> <pre><code>examples/\n\u251c\u2500\u2500 test-data.dat    # the raw VASE/CompleteEASE export\n\u2514\u2500\u2500 eln_data.yaml     # metadata the raw export doesn't carry, plus its own column layout\n</code></pre> <p>Open <code>test-data.dat</code>: the first three lines are a title and acquisition-method header, then a flat table \u2014 one row per (angle of incidence, wavelength) pair, repeated in blocks for each of the three measured angles (50\u00b0, 60\u00b0, 70\u00b0). Open <code>eln_data.yaml</code> too: besides the usual instrument/sample/user metadata, it also declares <code>colnames</code>, <code>sep</code>, and <code>skip</code> \u2014 the column layout <code>test-data.dat</code> needs to be parsed at all, since the raw export doesn't describe its own columns.</p>"},{"location":"tutorial/convert_your_first_dataset.html#steps","title":"Steps","text":""},{"location":"tutorial/convert_your_first_dataset.html#1-run-the-conversion","title":"1. Run the conversion","text":"<p>From the root of the repository:</p> <pre><code>pynx convert examples/eln_data.yaml examples/test-data.dat --reader ellips --nxdl NXellipsometry --output SiO2onSi_example.nxs\n</code></pre> <p>You're passing three things:</p> <ul> <li>the <code>.yaml</code> ELN file,</li> <li>the <code>.dat</code> raw data file,</li> <li><code>--reader ellips --nxdl NXellipsometry</code>, selecting <code>pynxtools-ellips</code>'s reader and the <code>NXellipsometry</code> application definition.</li> </ul> <p>The reader figures out which input file is which by extension, and reads the <code>.yaml</code> file first regardless of the order you pass them in \u2014 the <code>.dat</code> file's column layout depends on it (see Learn &gt; Reader architecture).</p>"},{"location":"tutorial/convert_your_first_dataset.html#2-read-the-output","title":"2. Read the output","text":"<p>The command prints a handful of warnings about missing documentation for a few fields and attributes \u2014 expected for this example, and they don't stop the conversion.</p> <p>At the end you should see:</p> <pre><code>The output file generated: SiO2onSi_example.nxs.\n</code></pre>"},{"location":"tutorial/convert_your_first_dataset.html#3-inspect-the-file","title":"3. Inspect the file","text":"<p><code>SiO2onSi_example.nxs</code> is a regular HDF5 file. Open it with H5Web in your browser, the VS Code H5Web extension, or any HDF5 viewer. You should find, among others:</p> <ul> <li><code>entry/data_collection/measured_data</code>: the full <code>(3, 2, 1088)</code> result array \u2014 3 angles, 2 observables (Psi, Delta), 1088 wavelength points.</li> <li><code>entry/data_collection/Psi_50deg</code>, <code>Delta_50deg</code>, ...: 1D virtual-dataset slices of <code>measured_data</code>, one per angle/observable \u2014 these are what the default plot actually shows, and why they have to be 1D rather than the full 3D array (see Learn &gt; Application definitions).</li> <li><code>entry/sample/name</code>: <code>2nm SiO2 on Si</code>, taken straight from <code>eln_data.yaml</code>.</li> <li>The default plot itself, rendered by any NeXus-aware viewer from <code>data_collection</code>'s <code>@signal</code>/<code>@axes</code>/<code>@auxiliary_signals</code> attributes.</li> </ul>"},{"location":"tutorial/convert_your_first_dataset.html#where-to-go-next","title":"Where to go next","text":"<ul> <li>If you want to understand what's actually happening during the conversion, Learn &gt; Reader architecture explains the design, and Learn &gt; Application definitions explains the NeXus concepts involved.</li> <li>For the bare command without the explanations, see How-to &gt; Convert data.</li> <li>If you're adding a second vendor format, Learn &gt; Reader architecture has the checklist.</li> </ul>"},{"location":"tutorial/installation.html","title":"Installation guide","text":""},{"location":"tutorial/installation.html#what-should-you-know-before-this-tutorial","title":"What should you know before this tutorial?","text":"<p>To get started, it does not hurt to read the following <code>pynxtools</code> tutorial:</p> <ul> <li>Guide on getting started with <code>pynxtools</code>, NeXus, and NOMAD</li> <li>Installation tutorial for <code>pynxtools</code></li> </ul>"},{"location":"tutorial/installation.html#what-you-will-know-at-the-end-of-this-tutorial","title":"What you will know at the end of this tutorial?","text":"<p>You will know</p> <ul> <li>how to install <code>pynxtools-ellips</code></li> <li>how to install <code>pynxtools-ellips</code> together with NOMAD</li> </ul>"},{"location":"tutorial/installation.html#setup","title":"Setup","text":"<p>It is recommended to use python 3.12 with a dedicated virtual environment for this package. Learn how to manage python versions and virtual environments.</p> <p>There are many alternatives to managing virtual environments and package dependencies (requirements). We recommend using <code>uv</code>, an extremely fast Python package and project manager. In this tutorial, you will find parallel descriptions, using either <code>uv</code> or a more classical approach using <code>venv</code> and <code>pip</code>.</p> <p>Start by creating a virtual environment:</p> uvvenv <p><code>uv</code> is capable of creating a virtual environment and install the required Python version at the same time.</p> <pre><code>uv venv --python 3.12\n</code></pre> <p>Note that you will need to install the Python version manually beforehand.</p> <pre><code>python -m venv .venv\n</code></pre> <p>That command creates a new virtual environment in a directory called <code>.venv</code>.</p>"},{"location":"tutorial/installation.html#installation","title":"Installation","text":"<p>Install the latest stable version of this package from PyPI with</p> uvpip <pre><code>uv pip install pynxtools-ellips\n</code></pre> <pre><code>pip install pynxtools-ellips\n</code></pre> <p>You can also install the latest development version with</p> uvpip <pre><code>uv pip install git+https://github.com/FAIRmat-NFDI/pynxtools-ellips.git\n</code></pre> <pre><code>pip install git+https://github.com/FAIRmat-NFDI/pynxtools-ellips.git\n</code></pre>"},{"location":"tutorial/installation.html#how-to-install-pynxtools-ellips-with-nomad","title":"How to install <code>pynxtools-ellips</code> with NOMAD","text":"<p>To use <code>pynxtools-ellips</code> with NOMAD, simply install it in the same environment as the <code>nomad-lab</code> package. NOMAD will recognize <code>pynxtools-ellips</code> as a plugin automatically. In addition, NOMAD will install a schema for NeXus application definitions.</p>"},{"location":"tutorial/installation.html#start-using-pynxtools-ellips","title":"Start using <code>pynxtools-ellips</code>","text":"<p>That's it! You can now use <code>pynxtools-ellips</code>!</p>"},{"location":"tutorial/standalone.html","title":"Convert ellipsometry (meta)data to NeXus","text":""},{"location":"tutorial/standalone.html#who-is-this-tutorial-for","title":"Who is this tutorial for?","text":"<p>This document is for people who want to standardize their research data by converting these into a NeXus standardized format.</p>"},{"location":"tutorial/standalone.html#what-should-you-know-before-this-tutorial","title":"What should you know before this tutorial?","text":"<ul> <li>You should have a basic understanding of FAIRmat NeXus and pynxtools</li> <li>You should have a basic understanding of using Python and Jupyter notebooks via JupyterLab</li> </ul>"},{"location":"tutorial/standalone.html#what-you-will-know-at-the-end-of-this-tutorial","title":"What you will know at the end of this tutorial?","text":"<p>You will have a basic understanding how to use pynxtools-ellips for converting your ellipsometry data to a NeXus/HDF5 file.</p>"},{"location":"tutorial/standalone.html#steps","title":"Steps","text":""},{"location":"tutorial/standalone.html#installation","title":"Installation","text":"<p>See the installation guide for how to install <code>pynxtools</code> together with the <code>ellips</code> reader plugin.</p>"},{"location":"tutorial/standalone.html#running-the-reader-from-the-command-line","title":"Running the reader from the command line","text":"<p>An example script to run the <code>ellips</code> reader in <code>pynxtools</code>:</p> <pre><code>pynx convert eln_data.yaml test-data.dat --reader ellips --nxdl NXellipsometry --output SiO2onSi.nxs\n</code></pre> <p>Both the ELN yaml and the vendor <code>.dat</code> file must be passed explicitly. The <code>eln_data.yaml</code> serves two purposes at once: it's the carrier for metadata that are typically entered in an ELN such as in NOMAD, and it declares the <code>.dat</code> file's column layout (<code>colnames</code>/<code>sep</code>/<code>skip</code>), which the reader needs to read <code>test-data.dat</code> at all \u2014 see Learn &gt; Reader architecture.</p>"},{"location":"tutorial/standalone.html#how-to-use-it","title":"How to use it?","text":"<p>Navigate to the examples directory. Therein, execute the following command which instructs the <code>ellips</code> reader to convert the example data using the <code>NXellipsometry</code> NeXus application definition resulting in a NeXus/HDF5 file:</p>"},{"location":"tutorial/standalone.html#examples","title":"Examples","text":"<p>You can find examples how to use <code>pynxtools-ellips</code> for your ellipsometry research data pipeline in <code>src/pynxtools_ellips/nomad/examples_uploads/example</code>. That example is designed for working with <code>NOMAD</code>.</p> <p>Congrats! You now have a FAIR NeXus file!</p>"}]}